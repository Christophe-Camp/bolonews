<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Bolo News{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('css/style.css') }}">
        {% endblock %}

        {% block javascripts %}
            
        {% endblock %}
    </head>
    <body>
        <header>
            <div class="nav-bar">
                <div class="logo">
                    <img src="logo.png" alt="bolonews" height="40">
                </div>
                <nav>
                    <ul>
                        <li><a href="{{ path('app_app') }}">Home</a></li>
                        <li><a href="{{ path('app_articles') }}">Articles</a></li>
                        <li><a href="{{ path('app_contact') }}">Contact</a></li>
                    </ul>
                </nav>
                <div class="auth-links">
                    {% if app.user %}
                        <a href="{{ path('app_user') }}">Mon espace</a>
                        <a href="{{ path('app_logout') }}">Déconnexion</a>
                    {% else %}
                        <a href="{{ path('app_login') }}">Se connecter</a>
                        <a href="{{ path('app_register') }}">M'inscrire</a>
                    {% endif %}
                </div>
            </div>
        </header>
        <main>
            <section class="hero-banner">
                <h1>Bolonews</h1>
                <p>Les meilleurs sauces pour les pâtes et de l"actualité sur le grill</p>
            </section>
            {% block body %}
            
                
            
            {% endblock %}
        </main>
        <footer>
            &copy; 2025 - Mon Site. Tous droits réservés.
        </footer>
    </body>
</html>
